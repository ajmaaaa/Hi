/**
 * Font CSS variable names — used in Tailwind classes and layout.
 * Fonts are loaded via Google Fonts <link> in app/layout.tsx.
 */
export const fontVariables = {
  fredericka: '--font-fredericka',
  libertinus: '--font-libertinus',
  imfell: '--font-imfell',
}

/**
 * Class string to apply to <html> — sets all CSS variable fallbacks.
 * The actual font values are set in globals.css @font-face / var definitions.
 */
export const htmlFontClass = ''
